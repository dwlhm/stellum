{
  "name": "try-elysia",
  "version": "1.0.50",
  "private": true,
  "scripts": {
    "test": "jest",
    "dev": "bun run --watch src/server.ts",
    "test:watch": "jest --watch"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ]
}
